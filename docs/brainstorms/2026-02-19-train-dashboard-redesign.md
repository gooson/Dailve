---
tags: [train, dashboard, suggested-workout, muscle-recovery, ux-redesign]
date: 2026-02-19
category: brainstorm
status: draft
---

# Brainstorm: Train 대시보드 전면 재설계 + 서제스티드 워크아웃 개선

## Problem Statement

현재 Train 대시보드의 3가지 핵심 문제:

1. **서제스티드 워크아웃 알고리즘 버그**: Compound 보강 로직(Step 4)에서 근육 회복 상태를 검증하지 않아, 방금 훈련한 근육을 포함하는 compound 운동이 추천됨 (예: 가슴운동 직후 벤치프레스 추천)
2. **정보 밀도와 우선순위 문제**: 서제스티드 카드가 최상단을 차지하지만 추천 근거가 불투명하고, 핵심 정보(회복 상태, 볼륨 진행도)가 분산되어 맥락 연결 부족
3. **시각적 일관성 부족**: 3개 섹션이 각각 독립적으로 설계되어 통일된 디자인 언어가 부재

## Target Users

- 주 3~5회 웨이트 트레이닝을 하는 중급 이상 사용자
- "오늘 뭘 해야 하지?"를 앱에서 빠르게 확인하고 싶은 사용자
- 근육 밸런스와 회복을 의식적으로 관리하는 사용자

## Success Criteria

1. 가슴운동 직후 가슴운동이 추천되지 않음 (알고리즘 버그 해결)
2. 대시보드 진입 시 3초 내에 "오늘 뭘 해야 하는지" 파악 가능
3. 근육 회복 상태가 시각적으로 즉시 인지 가능 (Hevy Body Diagram 수준)
4. 최근 운동 기록 → 회복 상태 → 추천 운동이 하나의 맥락으로 연결

## 경쟁 앱 분석

### Hevy
- **Body Diagram Heatmap**: 최근 7일 훈련 근육을 색상으로 표시 — 유일하게 근육별 회복 시각화 제공
- **Muscle Distribution**: 근육별 주간 세트 수 차트 + 신체 다이어그램
- **Quick Start**: Widget → 루틴 선택 → 즉시 시작 (0탭)
- 정보 밀도: 중~높음

### Strong
- **미니멀리스트**: 템플릿 중심, 빠른 시작이 핵심
- 근육 회복 시각화 없음
- Quick Start: 1-2탭
- 정보 밀도: 낮음

### Apple Fitness+
- **모듈러 카드**: 사용자가 섹션 순서/표시 커스터마이즈 가능
- **Training Load**: 전반적 훈련 부하만 표시 (근육별 아님)
- Activity Rings 최상단 배치
- 정보 밀도: 중간 (사용자 조절)

### 핵심 인사이트
- **근육별 회복 시각화**는 Hevy만 제공 — 차별화 포인트
- **추천의 근거를 시각적으로 보여주는 앱은 없음** — 우리가 선점 가능
- Quick Start까지의 friction 최소화가 공통 트렌드

---

## Proposed Approach: Recovery-Centered Dashboard

### 핵심 컨셉

> "근육 회복 맵이 대시보드의 히어로. 회복 상태가 곧 추천의 근거."

서제스티드 워크아웃을 독립 카드로 분리하지 않고, 근육 회복 맵 안에 자연스럽게 통합. 사용자가 "왜 이 운동을 추천하는지" 시각적으로 즉시 이해할 수 있도록.

### 새 대시보드 섹션 구조 (위→아래)

```
┌─────────────────────────────────┐
│  ① Weekly Progress Bar          │  ← 주간 목표 진행도 (compact)
│    ●●●○○  3/5 days this week   │
├─────────────────────────────────┤
│  ② Muscle Recovery Map          │  ← 히어로 섹션 (신체 다이어그램)
│    [Front/Back 인체 다이어그램]    │
│    색상: 🔴피로 🟡회복중 🟢회복완료 │
│                                 │
│    ┌─ Suggested ──────────────┐ │
│    │ 🟢 Back + Biceps         │ │  ← 회복된 근육 기반 추천
│    │ Pull-up · 4 sets         │ │
│    │ Barbell Row · 3 sets     │ │
│    │ [Start Workout →]        │ │
│    └──────────────────────────┘ │
├─────────────────────────────────┤
│  ③ Training Volume Summary      │  ← 28일 볼륨 차트 (기존 유지, compact)
│    [Stacked bar chart]          │
│    Last: 45min · 320kcal        │
├─────────────────────────────────┤
│  ④ Recent Workouts              │  ← 최근 기록 (기존 유지)
│    Today  · Back & Biceps       │
│    Yesterday · Chest & Triceps  │
│    ...                          │
│    [See All →]                  │
└─────────────────────────────────┘
```

### 각 섹션 상세

#### ① Weekly Progress Bar (NEW — compact)
- 기존 ActivityRing을 **가로 진행 바**로 대체 (공간 절약)
- `●●●○○ 3/5 days` 형태로 한 줄에 표시
- 탭 → TrainingVolumeDetailView (기존 동작 유지)

#### ② Muscle Recovery Map (NEW — 히어로)
- **상단**: 인체 다이어그램 (정면/후면 전환 가능)
  - 13개 MuscleGroup을 신체 위치에 매핑
  - 색상 코딩: 회복률 0~50% 빨강, 50~80% 노랑, 80~100% 초록
  - 미훈련 근육: 회색 (중립)
- **하단**: 회복 완료 근육 기반 추천
  - 현재 WorkoutRecommendationService 결과를 여기에 표시
  - "Back + Biceps" 같은 포커스 근육 라벨
  - 추천 운동 2~4개 + 세트 수
  - **[Start Workout →]** CTA 버튼
- **인터랙션**:
  - 근육 부위 탭 → 해당 근육 상세 (최근 훈련일, 주간 볼륨, 추천 운동)
  - Start Workout 탭 → ExerciseStartView sheet

#### ③ Training Volume Summary (KEEP — compact화)
- 기존 TrainingVolumeSummaryCard를 좀 더 compact하게
- ActivityRing 제거 (①로 이동)
- 28일 미니 바 차트 + 마지막 운동 메트릭(min, kcal)만 유지

#### ④ Recent Workouts (KEEP — 기존 유지)
- ExerciseListSection 기존 로직 유지
- limit 5개, See All 링크

---

## 알고리즘 버그 수정

### Bug 1: Compound 보강에서 recovery bypass

```swift
// 현재 (버그)
let compounds = library.allExercises()
    .filter { $0.primaryMuscles.count >= 2 || !$0.secondaryMuscles.isEmpty }
    .filter { $0.category == .strength }
    .filter { !usedIDs.contains($0.id) }

// 수정: 모든 primary muscle이 회복된 운동만 선택
let compounds = library.allExercises()
    .filter { $0.primaryMuscles.count >= 2 || !$0.secondaryMuscles.isEmpty }
    .filter { $0.category == .strength }
    .filter { !usedIDs.contains($0.id) }
    .filter { exercise in
        exercise.primaryMuscles.allSatisfy { muscle in
            fatigueStates[muscle]?.isRecovered == true
        }
    }
```

### Bug 2: `exercises.first` 고정 선택

```swift
// 현재: 다양성 없음
let exercise = exercises.first

// 수정: 랜덤 또는 최근 미수행 운동 우선
let exercise = exercises
    .sorted { $0.lastPerformed ?? .distantPast < $1.lastPerformed ?? .distantPast }
    .first  // 가장 오래 안 한 운동 우선
```

### 추가 개선: daysSince를 실제 시간 기반으로

```swift
// 현재: 정수 일수 × 24 (부정확)
let daysSince = Calendar.current.dateComponents([.day], from: date, to: now).day ?? 0
let hoursSince = Double(daysSince) * 24.0

// 개선: 실제 경과 시간
let hoursSince = max(0, now.timeIntervalSince(date) / 3600.0)
```

---

## Constraints

### 기술적 제약
- **Body Diagram 에셋 필요**: 13개 근육 그룹별 SVG/Shape 레이어
  - Option A: SF Symbols `figure.strengthtraining.traditional` 계열 활용 (제한적)
  - Option B: 커스텀 SwiftUI Shape로 직접 그리기 (유연하지만 공수 큼)
  - Option C: 간소화된 근육 그리드 (다이어그램 대신 그리드 레이아웃)
- **MuscleGroup ↔ 신체 위치 매핑**: 13개 그룹의 좌표 데이터 필요
- 기존 WorkoutRecommendationService 리팩토링 범위 제한 필요

### 시간/리소스 제약
- Body Diagram을 MVP에 포함 — 에셋 제작 방안 결정 필요
- 커스텀 SwiftUI Shape 또는 외부 에셋 활용

---

## Edge Cases

1. **첫 사용자 (운동 기록 0건)**: 모든 근육 "회복 완료" 상태 → 전신 운동 추천
2. **모든 근육 피로 상태**: 추천 없음 → "Rest Day" 메시지 + 스트레칭/유산소 제안
3. **HealthKit만 있고 수동 기록 없음**: 근육 매핑 불가 → 일반적 추천 또는 맵 비활성화
4. **운동 라이브러리에 해당 근육 운동이 없음**: 빈 추천 → compound 운동으로 대체 (recovery 검증 포함)

---

## Scope

### MVP (Must-have)
- [ ] 추천 알고리즘 버그 수정 (compound recovery bypass)
- [ ] 추천 다양성 개선 (exercises.first → 최근 미수행 우선)
- [ ] daysSince를 실제 시간 기반으로 변경
- [ ] Weekly Progress Bar (compact, 상단)
- [ ] **Muscle Recovery Map** (Body Diagram 시각화)
  - 실루엣 기반 인체 다이어그램 (정면/후면)
  - 13개 MuscleGroup을 신체 위치에 매핑, 색상 코딩 (회복 상태)
  - 추천 운동을 맵 아래에 통합
- [ ] 근육 부위 탭 → 상세 팝오버 (최근 훈련일, 볼륨 트렌드)
- [ ] 추천 운동 스와이프로 대안 보기
- [ ] 요일/시간대 패턴 반영 (월요일에 주로 가슴 했으면 월요일에 가슴 추천)
- [ ] Training Volume Summary compact화
- [ ] 섹션 순서 재배치

### Future
- [ ] 커스터마이즈 가능한 카드 시스템 (Apple Fitness+ 스타일)
- [ ] HealthKit cardio 기록의 전신 피로도 반영
- [ ] 운동 강도(RPE, 무게) 기반 피로도 정밀화
- [ ] AI 기반 개인화 추천 (패턴 학습)

---

## Open Questions

1. ~~**Body Diagram vs Grid**~~ → **결정됨: Body Diagram MVP 포함**
2. ~~**Rest Day 추천**~~ → **결정됨: 긍정적 프레이밍 + Active Recovery 제안** — "Recovery in progress" 메시지 + Walking/Stretching/Yoga 3가지 카드 + 다음 훈련 가능 시점 표시 ("Chest ready in ~24h")
3. **Weekly Progress**: 기존 Activity Ring(원형) vs 새 Progress Bar(선형) — 어떤 형태가 더 적절한가?
4. ~~**근육 회복 시간**~~ → **결정됨: 근육 그룹별 차등** (예: 하체/등 72h, 가슴/어깨 48h, 이두/삼두/전완 24~36h)
5. ~~**Body Diagram 에셋 방안**~~ → **결정됨: 오픈소스 활용** — `react-native-body-highlighter` (MIT 라이선스)에서 SVG path 추출 → SwiftUI Shape 변환. lats 등 누락 근육은 Wikimedia Commons SVG로 보완

---

## Next Steps

- [ ] `/plan` 으로 구현 계획 생성 (MVP 범위 — Body Diagram 포함)
- [ ] Body Diagram 에셋 방안 결정 (Shape vs SVG vs SF Symbol)
- [ ] 알고리즘 버그 수정 선행 (독립적으로 진행 가능)
