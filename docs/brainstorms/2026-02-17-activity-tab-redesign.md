---
tags: [activity, exercise, strength-training, ux-redesign, muscle-group, calorie, set-rep-weight]
date: 2026-02-17
category: brainstorm
status: draft
---

# Brainstorm: Activity 탭 UX 전면 개편

## Problem Statement

현재 Activity 탭은 **모든 운동을 동일한 폼**(Duration + Calories + Distance + Memo)으로 기록한다. 근력 운동이 "Strength" 하나로 통합되어 있고, 세트/렙/무게 기록이 불가능하며, 이전 기록 참조·근육 그룹 추적·자동 칼로리 추정이 없다.

업계 선두 앱(Strong, Hevy, Fitbod)은 **운동 타입별 맞춤 입력**, **이전 세션 인라인 표시**, **휴식 타이머**, **근육 그룹 시각화**를 기본 제공한다.

## Target Users

- 주 3-5회 헬스장에서 근력 운동하는 사용자
- Apple Watch를 착용하고 HealthKit 데이터가 있는 사용자
- 운동 기록을 통해 progressive overload를 추적하고 싶은 사용자
- 코칭(AI 추천) 기능을 원하는 사용자 (Fitbod 스타일)

## Success Criteria

1. 근력 운동 1세트 기록에 **3탭 이하** (무게 입력 → 렙 확인 → 체크)
2. 이전 세션 기록이 **항상 인라인 표시**되어 progressive overload 즉시 파악
3. 운동 타입별 **맞춤 입력 필드** 자동 전환
4. **100+ 운동 라이브러리** 제공 (근육 그룹별 분류)
5. HealthKit 칼로리 우선, 없으면 **MET 기반 자동 추정**

## Design Direction: Fitbod 스타일 코칭 중심

사용자 선택: **코칭 중심 (Fitbod 스타일)**

핵심 차별점:
- AI가 피로도/볼륨 기반으로 오늘의 운동 추천
- 근육 그룹별 회복 상태 시각화
- 자동 프로그램 생성 (목표/빈도 기반)
- 단, MVP에서는 기록 인프라 우선 → 코칭은 Phase 2

## Proposed Approach

### 1. 운동 타입별 맞춤 입력 시스템

| 운동 카테고리 | 입력 필드 | 예시 |
|-------------|----------|------|
| **근력 (Strength)** | 세트 × 렙 × 무게(kg) | Bench Press: 3×10×60kg |
| **유산소 (Cardio)** | 시간 + 거리 + 평균 심박 | Running: 30min, 5km |
| **HIIT** | 라운드 × 시간 + 휴식 시간 | Tabata: 8R × 20s/10s |
| **유연성 (Flexibility)** | 시간 + 강도(1-10) | Yoga: 60min, 강도 5 |
| **바디웨이트** | 세트 × 렙 (무게 optional) | Pull-up: 4×8 |

### 2. 운동 라이브러리 구조 (100+ 종)

**근육 그룹 분류**:
- 가슴 (Chest): Bench Press, Incline Press, Cable Fly, Dumbbell Fly, Push-up...
- 등 (Back): Deadlift, Pull-up, Lat Pulldown, Barbell Row, Cable Row...
- 어깨 (Shoulders): OHP, Lateral Raise, Face Pull, Rear Delt Fly...
- 팔 (Arms): Bicep Curl, Tricep Extension, Hammer Curl, Skull Crusher...
- 하체 (Legs): Squat, Leg Press, Leg Curl, Leg Extension, Lunge, Calf Raise...
- 코어 (Core): Plank, Crunch, Leg Raise, Russian Twist, Ab Wheel...
- 전신 (Full Body): Clean, Snatch, Thruster, Burpee...

**장비 분류**:
- 바벨 (Barbell)
- 덤벨 (Dumbbell)
- 머신 (Machine)
- 케이블 (Cable)
- 바디웨이트 (Bodyweight)
- 밴드 (Band)
- 케틀벨 (Kettlebell)

**데이터 모델 (운동 정의)**:
```
Exercise {
    id: UUID
    name: String (en)
    localizedName: String (ko)
    category: ExerciseCategory  // strength, cardio, hiit, flexibility, bodyweight
    primaryMuscles: [MuscleGroup]
    secondaryMuscles: [MuscleGroup]
    equipment: Equipment
    metValue: Double  // for calorie estimation
    instructions: String?
    imageAsset: String?
}
```

### 3. 세트/렙/무게 입력 UX (Strong/Hevy 참조)

```
┌─────────────────────────────────────┐
│  Bench Press                    ✕   │
│  가슴 · 바벨                         │
├─────────────────────────────────────┤
│  이전 세션: 2/15  60kg×10, 65kg×8    │  ← 이전 기록 참조
├─────┬──────┬──────┬────────┬────────┤
│ SET │  이전  │ KG   │  REPS  │   ✓   │
├─────┼──────┼──────┼────────┼────────┤
│  1  │ 60×10│  60  │   10   │  [✓]  │  ← 이전 값 자동 채움
│  2  │ 65×8 │  65  │    8   │  [ ]  │
│  3  │ 65×8 │  65  │    8   │  [ ]  │
│  + 세트 추가                         │
├─────────────────────────────────────┤
│          ⏱ 휴식: 1:30               │  ← 세트 체크 시 자동 시작
└─────────────────────────────────────┘
```

핵심 UX 원칙:
- **이전 값 자동 채움**: 지난 세션의 무게/렙이 기본값으로 입력됨
- **탭 최소화**: 변경 없으면 체크만 탭 (1탭)
- **무게 증감**: +/- 버튼 또는 직접 입력
- **세트 타입**: Long press로 Warmup/Drop/Failure 전환
- **휴식 타이머**: 세트 완료 시 자동 시작, 커스텀 시간 설정 가능

### 4. 칼로리 계산: 하이브리드 방식

**우선순위**:
1. HealthKit의 `activeEnergyBurned` (Apple Watch 착용 시)
2. MET 기반 추정 (HealthKit 없을 때)

**MET 기반 추정 공식**:
```
Calories = MET × bodyWeight(kg) × duration(hours)
```

**운동별 MET 값 예시**:
| 운동 | MET |
|------|-----|
| Bench Press (보통 강도) | 3.5 |
| Squat (높은 강도) | 6.0 |
| Deadlift | 6.0 |
| Running (8km/h) | 8.3 |
| Walking (5km/h) | 3.5 |
| HIIT | 8.0 |
| Yoga | 2.5 |

**근력 운동 특화 추정**:
- 실제 운동 시간 = 총 세션 시간 - 총 휴식 시간
- 무게 기반 강도 보정: (현재 무게 / 추정 1RM) 비율로 MET 조정
- 체중은 HealthKit에서 최신값 fetch

### 5. Activity 탭 레이아웃 개편안

```
┌─────────────────────────────────────┐
│  Activity                     [+]   │
├─────────────────────────────────────┤
│  ┌─────────────────────────────┐    │
│  │  🏋️ 오늘의 추천 운동            │    │  ← Phase 2: AI 코칭
│  │  가슴 + 삼두 (회복 완료)        │    │
│  │  [운동 시작]                  │    │
│  └─────────────────────────────┘    │
├─────────────────────────────────────┤
│  주간 요약          [Exercise|Steps] │
│  ▇ ▇▇ ▇▇▇ ▇▇ ▇▇▇ ▇  ▇▇           │  ← 기존 차트 유지
│  Mon Tue Wed Thu Fri Sat Sun        │
├─────────────────────────────────────┤
│  근육 그룹 맵                        │
│  ┌────────────┐                     │
│  │  [인체 앞면] [인체 뒷면]  │         │  ← Phase 2: 근육 시각화
│  │  가슴: 48h 전  등: 오늘    │         │
│  └────────────┘                     │
├─────────────────────────────────────┤
│  오늘                                │
│  ┌──────────┐ ┌──────────┐          │
│  │ 🏃 운동    │ │ 👟 걸음    │          │  ← 기존 카드 유지
│  │ 45 min   │ │ 8,234    │          │
│  └──────────┘ └──────────┘          │
├─────────────────────────────────────┤
│  최근 운동                     전체 > │
│  ┌─────────────────────────────┐    │
│  │ 🏋️ Bench Press  오늘 14:30   │    │  ← 세트 요약 추가
│  │    3세트 · 60-65kg · 26렙     │    │
│  │    추정 180 kcal              │    │
│  ├─────────────────────────────┤    │
│  │ 🏃 Running      오늘 07:00   │    │
│  │    30min · 5.2km · 320 kcal  │    │
│  └─────────────────────────────┘    │
└─────────────────────────────────────┘
```

## Constraints

### 기술적 제약
- **SwiftData**: ExerciseRecord 모델 마이그레이션 필요 (세트/렙/무게 추가)
- **HealthKit**: 읽기 전용 — HealthKit에 세트 데이터 쓸 수 없음
- **CloudKit**: 데이터 구조 변경 시 전 디바이스 동기화 고려
- **iOS 26+**: 최신 API 사용 가능 (장점)

### 시간/리소스 제약
- 1인 개발 — Phase 분리 필수
- 운동 라이브러리 데이터 직접 구축 필요 (100+ 항목)
- 근육 시각화 에셋 필요 (Phase 2)

## Edge Cases

- **운동 중 앱 종료**: 진행 중인 세트 데이터 로컬 저장 + 복구
- **HealthKit 권한 거부**: MET 기반 추정으로 fallback
- **체중 데이터 없음**: 기본 70kg 사용 + 입력 유도
- **커스텀 운동**: 사용자가 운동명, 근육 그룹, 장비 직접 지정
- **단위 전환**: kg ↔ lb 설정 지원
- **이전 기록 없는 첫 세션**: 빈 상태에서 가이드 표시

## Scope

### MVP (Phase 1) — Must-have
- [ ] 운동 타입별 맞춤 입력 폼 (근력: 세트/렙/무게, 유산소: 시간/거리)
- [ ] 기본 운동 라이브러리 (100+ 종, 근육 그룹/장비 분류)
- [ ] 이전 세션 기록 인라인 표시
- [ ] 휴식 타이머 (세트 완료 시 자동 시작)
- [ ] 근육 그룹 태깅 (운동별 주요/보조 근육)
- [ ] MET 기반 칼로리 자동 추정 (HealthKit fallback)
- [ ] ExerciseRecord 모델 확장 (sets, reps, weight 배열)
- [ ] Activity 탭 UI 개편 (최근 운동에 세트 요약 표시)

### Phase 2 — Nice-to-have
- [ ] AI 기반 오늘의 운동 추천 (근육 피로도 + 주간 볼륨 기반)
- [ ] 근육 그룹 시각화 (인체 다이어그램)
- [ ] 세트 타입 분류 (Warmup, Drop, Failure, Superset)
- [ ] 운동 루틴/템플릿 저장 및 재사용
- [ ] Progressive overload 차트 (운동별 무게/볼륨 추세)
- [ ] Apple Watch 컴패니언 앱 (세트 기록)
- [ ] 운동 설명/이미지/동영상

### Phase 3 — Future
- [ ] 소셜 기능 (운동 공유)
- [ ] 1RM 추정 및 강도 기반 프로그래밍
- [ ] 주간/월간 볼륨 분석 리포트
- [ ] 슈퍼세트/서킷 지원
- [ ] 커스텀 운동 생성 UI

## Data Model 변경안

### 기존 ExerciseRecord 확장

```swift
// 새로운 모델: WorkoutSet (세트별 데이터)
@Model final class WorkoutSet {
    var id: UUID
    var exerciseRecord: ExerciseRecord  // relationship
    var setNumber: Int
    var setType: SetType  // .warmup, .working, .drop, .failure
    var weight: Double?   // kg
    var reps: Int?
    var duration: TimeInterval?  // 유산소/플랭크용
    var distance: Double?        // 유산소용 (meters)
    var isCompleted: Bool
    var restDuration: TimeInterval?
}

enum SetType: String, Codable {
    case warmup, working, drop, failure
}

// ExerciseRecord 확장
@Model final class ExerciseRecord {
    // 기존 필드 유지 (하위 호환)
    var id: UUID
    var date: Date
    var exerciseType: String
    var duration: TimeInterval
    var calories: Double?
    var distance: Double?
    var memo: String
    var isFromHealthKit: Bool
    var healthKitWorkoutID: String?
    var createdAt: Date

    // 새 필드
    @Relationship(deleteRule: .cascade) var sets: [WorkoutSet]
    var exerciseID: String?          // 라이브러리 운동 ID 참조
    var primaryMuscles: [String]     // 근육 그룹 태깅
    var secondaryMuscles: [String]
    var equipment: String?
    var estimatedCalories: Double?   // MET 기반 추정값
    var calorieSource: CalorieSource // .healthKit, .met, .manual
}

enum CalorieSource: String, Codable {
    case healthKit, met, manual
}
```

### 운동 라이브러리 (정적 데이터)

```swift
// 앱 번들에 포함되는 정적 운동 정의
struct ExerciseDefinition: Codable, Identifiable {
    let id: String           // "bench-press", "squat" 등
    let name: String
    let localizedName: String
    let category: ExerciseCategory
    let primaryMuscles: [MuscleGroup]
    let secondaryMuscles: [MuscleGroup]
    let equipment: Equipment
    let metValue: Double
    let inputType: ExerciseInputType  // .setsRepsWeight, .durationDistance, .setsReps, .durationIntensity
}

enum ExerciseCategory: String, Codable {
    case strength, cardio, hiit, flexibility, bodyweight
}

enum MuscleGroup: String, Codable, CaseIterable {
    case chest, back, shoulders, biceps, triceps
    case quadriceps, hamstrings, glutes, calves
    case core, forearms, traps, lats
}

enum Equipment: String, Codable, CaseIterable {
    case barbell, dumbbell, machine, cable
    case bodyweight, band, kettlebell, other
}

enum ExerciseInputType: String, Codable {
    case setsRepsWeight    // 근력: 세트 × 렙 × 무게
    case setsReps          // 바디웨이트: 세트 × 렙
    case durationDistance   // 유산소: 시간 + 거리
    case durationIntensity  // 유연성: 시간 + 강도
    case roundsBased       // HIIT: 라운드 × 시간
}
```

## Open Questions

1. **운동 라이브러리 데이터 소스**: JSON 번들 vs 외부 API vs 직접 구축?
2. **단위 설정**: kg/lb 전환 UI 위치? (설정 화면 vs 인라인?)
3. **기존 데이터 마이그레이션**: 기존 "Strength" 기록은 어떻게 처리? (sets 없는 레거시)
4. **운동 검색 UX**: 최근 사용 우선 vs 알파벳 vs 근육 그룹별 탭?
5. **휴식 타이머 위치**: 화면 하단 고정 vs 모달 vs 알림?

## 레퍼런스 앱 분석 요약

| 앱 | 장점 (채택) | 단점 (회피) |
|----|-----------|-----------|
| **Strong** | 최소 탭 입력, 이전 기록 회색 텍스트, 플레이트 계산기 | 소셜 없음, 운동 수 제한 |
| **Hevy** | 세트 타입 분류, 무료 기능 풍부, 현대적 UI | 과도한 소셜 요소 |
| **Fitbod** | AI 추천, 근육 피로도 시각화, 자동 프로그래밍 | 학습 곡선, 유료 |
| **JEFIT** | 1,400+ 운동, 근육 다이어그램 | 구식 UI, 광고 |

## Next Steps

- [ ] `/plan activity-tab-redesign` 으로 Phase 1 구현 계획 생성
- [ ] 운동 라이브러리 JSON 데이터 구축 (100+ 종)
- [ ] ExerciseRecord SwiftData 마이그레이션 전략 확정
